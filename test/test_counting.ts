import * as assert from 'assert';

describe('Test 1',()=>{
  it('This value should be equal 2', ()=>{
    assert(2 === 2);
  });
    it('This value should be equal 3', ()=>{
      assert(3 === 3);
      console.log('Test 2')
    });
});